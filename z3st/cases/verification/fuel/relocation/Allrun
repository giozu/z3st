#!/bin/bash
cd "${0%/*}" || exit 1

set -e
# Fragmented-pellet relocation case (Phase 1: dedicated dolfinx_contact solver).
# Requires dolfinx_contact built into the env (third_party/dolfinx_contact/build.sh).
python3 make_mesh.py                       # fragmented fuel + cladding mesh
python3 relocation_solve.py                # multi-body contact + relocation
