# Documentation build dependencies for Z3ST (Sphinx).
#
#   pip install -r docs/requirements.txt
#   cd docs && make clean html
#
# The FEniCSx runtime stack (dolfinx, basix, ufl, mpi4py, petsc4py, pyvista,
# h5py, gmsh) is not needed here: it is mocked via `autodoc_mock_imports` in
# docs/source/conf.py, so the docs build in a plain environment.
#
# Version floors are the ones verified to build the docs cleanly; they are lower
# bounds, not exact pins.

sphinx>=7.2
myst-parser>=3.0
sphinx-book-theme>=1.1
sphinx-rtd-theme>=2.0
sphinx-autodoc-typehints>=2.0
sphinxcontrib-mermaid>=1.0
